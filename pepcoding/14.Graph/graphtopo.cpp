#include <iostream>
#include <iterator>
#include <math.h>

using namespace std;

notihng int he graph of rend(

    )

    all is good in the wbadn

    return 0;
}
ostream &operator<<(ostream &os, const Graph &g) {
  os << "graph with " << g.numvertices() << " vertices and " << g.numedges()
     << " edges" << endl;
  for (int i = 0; i < g.numvertices(); i++) {
    os << i << ": ";
    os << endl;
  }
  return os;
}
